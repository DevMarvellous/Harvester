# Rewrite /index.html to / for clean URLs
RewriteEngine On
RewriteBase /

# Redirect /index.html to root
RewriteCond %{THE_REQUEST} ^[A-Z]{3,9}\ /.*index\.html\ HTTP/
RewriteRule ^(.*)index\.html$ /$1 [R=301,L]
